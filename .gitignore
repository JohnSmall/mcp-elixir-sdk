/_build/
/cover/
/deps/
/doc/
/tmp/
erl_crash.dump
*.ez
mcp_elixir_sdk-*.tar

# Node tooling used by the reviewer (Codex); not part of this Elixir SDK (MES-26 CO-1)
/node_modules/
package.json
package-lock.json
.claude
.mcp.json


# `mix credo --debug` drops this at the repo root. With MES-51 making a dirty
# worktree a refusal condition, an untracked debug artefact would false-red a
# perfectly good conformance run.
/credo-debug-log.html
